# G2 Launcher 0.4.12 release build

This branch reconstructs the editable source, validates the image-transport safety markers, builds the Vite bundle, checks JavaScript syntax, packs the EHPK, and publishes both release files as a workflow artifact.
