# v0.2.26 HUD event-capture and card size fix

Built on v0.2.25.

Changes:
- Moved event capture off the visible footer into a 1x1 invisible capture layer, so the footer text no longer appears scrollable.
- Restored visible footer guidance as a non-capturing text container.
- Enlarged HUD cards and widened the selected card.
- Increased title clamp length so more of the selected item title can be shown/wrapped.
- Added in-place text upgrades while scrolling between menu elements, so the logo is not repainted on every selection change.
