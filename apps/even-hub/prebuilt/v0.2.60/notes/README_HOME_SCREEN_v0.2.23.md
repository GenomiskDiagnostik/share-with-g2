# Send to G2 v0.2.23 — HUD card layout fix

Changes:

- Removed remaining decorative card/icon glyphs from the G2 HUD home screen.
- Header remains `Delt indbakke x/y` / `Shared inbox x/y`.
- Reworked cards as separate border and text containers so labels are vertically centered instead of clipped at the bottom.
- Reduced card height and moved card text to the visual center.
- Preserved R1 behavior: scroll moves selection, single tap opens, double tap remains back/delete semantics.
