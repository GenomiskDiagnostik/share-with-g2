# v0.2.29 HUD dots cleanup

Built from v0.2.28.

Changes:
- Removed the ellipsis suffix from truncated HUD card labels. Long labels are now hard-clipped without adding three dots on the right side of the glasses UI.
- Kept clock/header/logo/card/footer layout unchanged.
- R1 behaviour unchanged: scroll moves selection, single tap opens, double tap keeps back/delete semantics.
